import item from './item.js';

const store = {
  items: [],
  hideCheckedItems: false
};

export default {
items: store.items,
hideCheckedItems: store.hideCheckedItems
};

console.log(item);
console.log(foo);